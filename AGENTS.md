# AGENTS.md

이 저장소에서 작업하는 모든 AI 에이전트가 따르는 규칙이다. 도구에 상관없이 이 파일을 먼저 읽는다.

## 이 프로젝트가 무엇인가

인사, 근태, 노무, 4대보험, 기타소득, 세무, 교육을 하나의 사원 데이터 위에서 처리하는 관리 시스템. 학습 목적의 개인 프로젝트다.

**현재 단계는 요구사항 정의다. 코드는 아직 한 줄도 없다.** 기술 스택도 확정되지 않았다. 요청받지 않은 채로 코드를 만들거나 스택을 고르지 않는다.

## 시작하기 전에 읽을 것

1. `specs/000-product/spec.md` — 제품 원칙 8개와 확정된 설계 결정. 나머지 전부의 전제다.
2. `specs/900-플랫폼기반/spec.md` — 도메인 스펙이 "결재를 거쳐 승인된다", "권한 범위 안에서만 조회된다"고 쓸 때 그 실체.
3. 작업할 도메인의 `specs/0N0-*/spec.md`.

## 스펙 주도 개발

기능 작업은 `specs/<NNN-slug>/` 아래 세 문서를 순서대로 만들고, 코드는 그 문서를 근거로만 쓴다.

| 단계 | 산출물 | 하는 일 | 하지 않는 일 |
|---|---|---|---|
| 1 Specify | `spec.md` | 사용자, 문제, 목표와 비목표, 시나리오, FR·AC·EC, 성공 기준 | 기술 스택과 라이브러리, 테이블 설계 언급 |
| 2 Plan | `design.md` | 아키텍처, 스택, 데이터 모델, API, 권한, 보안, 성능 | 코드 작성 |
| 3 Tasks | `tasks.md` | T-001 형식으로 작업 분해, 각 작업에 요구사항 ID 연결 | 여러 목적을 한 작업에 묶기 |
| 4 Implement | 코드 | 지정된 작업 하나만 구현 | 스펙에 없는 기능 추가 |
| 5 Verify | 검증 표 | diff를 스펙과 대조 | 돌아가니까 통과라는 판정 |

**Specify** — 기술 이야기를 최대한 늦춘다. 모호한 부분은 임의로 채우지 말고 열린 질문에 남긴다.

**Implement** — 요청받은 작업 범위를 벗어나지 않는다. 변경할 파일 목록을 먼저 제안한다. 불확실한 부분은 임의로 결정하지 말고 가정으로 명시한다.

**Verify** — 만족한 요구사항, 누락된 요구사항, 스펙에 없는 임의 구현, 엣지 케이스 누락, 테스트 부족, 보안과 권한 위험, 회귀 가능성을 요구사항 ID와 연결한 표로 보고한다.

**스펙과 코드가 어긋날 때** — 코드를 먼저 바꾸지 않는다. 스펙을 고치고 변경 근거를 남긴 뒤 구현한다.

## 스펙 작성 규칙

**ID 체계** — FR 기능 요구사항, AC 수용 기준, EC 엣지 케이스, NFR 비기능 요구사항, T 작업. 번호는 재사용하지 않는다. 항목을 삭제해도 번호는 비워 둔다.

**모든 FR에 AC가 1대1로 붙는다.** 요구사항 바로 아래 인용문으로 둔다. 뒤에 표로 몰아 두지 않는다. 그래야 판정 기준이 빠진 항목이 눈에 보인다.

**테스트 가능한 문장으로 쓴다.** 쉽게, 빠르게, 적절히, 좋게는 금지어다. 성공과 실패를 판정할 수 없으면 요구사항이 아니다.

**AC는 EARS 문법으로 쓴다.** 한국어로 쓰되 키워드는 영문 대문자를 유지한다.

| 패턴 | 형식 |
|---|---|
| 항상 적용 | THE 시스템 SHALL 모든 비밀번호를 해시하여 저장한다 |
| 이벤트 기반 | WHEN 사용자가 올바른 인증 정보를 제출하면, THE 시스템 SHALL 로그인 처리한다 |
| 상태 기반 | WHILE 세션이 활성 상태이면, THE 시스템 SHALL 보호된 페이지 접근을 허용한다 |
| 오류와 예외 | IF 결제 승인에 실패하면, THEN THE 시스템 SHALL 주문을 생성하지 않는다 |
| 선택 기능 | WHERE 2단계 인증이 켜져 있으면, THE 시스템 SHALL 추가 코드를 요구한다 |

기존 문서의 형식을 그대로 따른다. 예시는 `specs/010-인사관리/spec.md`를 본다.

## 어기면 안 되는 제품 원칙

전문은 `specs/000-product/spec.md` 6절에 있다. 요약하면 이렇다.

- **단일 원천** — 사원 정보는 인사관리에만 있다. 다른 도메인은 복제하지 않고 참조한다.
- **요율 외부화** — 보험 요율, 세액표, 최저임금, 법정 기한을 코드 상수로 두지 않는다. 연도별 테이블에 둔다.
- **계산 근거 보존** — 급여·보험료·세액 결과에 사용한 요율과 입력값 스냅샷을 함께 저장한다. 과거 기간 재계산 시 당시 규칙이 적용되어야 한다.
- **파생값 금지** — 연차 잔여일처럼 계산으로 얻는 값은 컬럼을 직접 수정하지 않는다. 이력의 차이로 산출한다.
- **서버 권한 강제** — 화면 숨김에 의존하지 않는다. 조회 범위와 쓰기 권한을 서버에서 다시 검증한다.
- **민감정보 3중 보호** — 암호화 저장, 목록 마스킹, 열람 이력 기록을 모두 적용한다.
- **AI 경계** — AI에 개인 식별 정보를 전달하지 않는다. 집계값과 익명 식별자만 넘기고 결과는 초안으로만 취급한다.
- **확정 불변** — 급여 마감, 신고 확정, 결재 승인 이력은 사후 수정하지 않는다. 정정 기록을 새로 남긴다.

금액 계산에 부동소수점을 쓰지 않는다. 정수 최소 단위로 다룬다.

## 확정된 설계 결정

바꾸려면 먼저 `specs/000-product/spec.md`를 고치고 근거를 남긴다.

- 개발 기간 제약 없음. 단계별 기능 절단선을 두지 않는다.
- 단일 기업 모델. 회사 1개 아래 사업장과 부서. 대행 사무소 계층 없음.
- 겸직 없음. 사원은 사업장 1개, 부서 1개에만 소속.
- 급여는 060 세무관리에 포함. 별도 도메인을 두지 않는다.

## 아직 비어 있는 곳

**급여 계산 요구사항이 없다.** `specs/060-세무관리/spec.md` 5.6절에 범위만 잡혀 있다. 근태의 시간 구분, 보험의 사원 부담분, 세무의 원천징수가 모두 급여를 거쳐 흐르므로 계산 사슬 한가운데가 비어 있다.

제품 수준 열린 질문 5개와 도메인별 질문 41개가 각 스펙 문서에 남아 있다. **열린 질문을 임의로 닫지 않는다.** 판단이 필요하면 사람에게 묻는다.

## Git 브랜치와 AI 협업 규칙

이 저장소는 Cursor, Codex, 사람이 함께 작업한다. 우아한형제들의 Git-flow 운영 원칙을 현재 규모에 맞게 단순화해 적용한다.

### 역할

- **사용자** — 우선순위와 제품 결정을 확정하고 스펙 충돌의 최종 판단을 한다.
- **Cursor** — 배정받은 티켓을 독립 작업 브랜치에서 작성하거나 구현한다.
- **Codex** — 배정받은 티켓을 작업하고, 최종 통합 전에 전체 diff·스펙·테스트·충돌을 검증하는 기본 통합 담당자다.
- 역할은 소유권이 아니다. 티켓에 담당자가 명시되면 그 담당자가 해당 범위의 유일한 작성자다.

### 브랜치 종류

| 브랜치 | 목적 | 생성 기준 | 병합 대상 |
|---|---|---|---|
| `main` | 언제든 배포 가능한 안정 상태 | 항상 유지 | 직접 작업 금지 |
| `develop` | 다음 배포의 통합 상태 | `main`에서 최초 생성 | `release/*` |
| `feature/<AGENT>-<TICKET>-<slug>` | 기능·스펙·일반 수정 | 최신 `develop` | `develop` |
| `release/<version>` | 통합 검증과 QA 수정 | `develop` | `main`, 이후 `develop` 동기화 |
| `hotfix/<TICKET>-<slug>` | 운영 긴급 수정 | 최신 `main` | `main`, 이후 `develop` 동기화 |

브랜치 예:

```
feature/CURSOR-HR-101-employee-profile
feature/CODEX-TAX-203-withholding-validation
release/0.1.0
hotfix/HR-901-mask-sensitive-data
```

브랜치 이름의 에이전트 표기는 `CURSOR` 또는 `CODEX`를 사용한다. 문서·운영 규칙만 바꾸는 작업은 `docs/<TICKET>-<slug>`를 허용한다.

### 단일 요청 자동화

`agent:pair`와 `status:ready` 라벨이 붙은 GitHub Issue는 공동 작업 요청이다. Cursor가 먼저 구현하고 Codex가 검증·통합한다. 두 에이전트가 같은 기능을 중복 구현하지 않는다. 상태 전이와 예외 처리는 `.github/AI_AUTOMATION.md`를 따른다.

### 작업 흐름

1. 작업 전에 GitHub Issue 또는 `tasks.md`에 티켓을 만든다.
2. 티켓에 담당 에이전트, 수정할 스펙/파일, 관련 FR·AC, 완료 조건을 적는다.
3. 최신 기준 브랜치에서 작업 브랜치를 만든다.
4. 한 브랜치는 한 티켓만 처리한다. 같은 파일을 두 AI에게 동시에 배정하지 않는다.
5. 작업 브랜치에서 작은 검증을 수행하고 의미 없는 중간 커밋은 정리한다.
6. 푸시 직전에 원격을 가져와 기준 브랜치 위로 rebase한다.
7. 작업 브랜치를 push하고 PR을 연다. `feature/*`는 `develop`을 대상으로 한다.
8. 담당자가 아닌 다른 AI가 스펙 대비 diff와 테스트를 리뷰한다.
9. 사용자 승인 또는 사용자에게 위임받은 통합 담당자의 승인 후 squash merge한다.
10. 배포 대상 작업이 모두 `develop`에 모이면 `release/<version>`에서 전체 검증한다.
11. 검증이 끝난 release만 `main`에 병합하고 버전 태그를 단다.

```bash
git fetch origin
git switch develop
git pull --ff-only origin develop
git switch -c feature/CODEX-TICKET-slug

# 작업 후
git fetch origin
git rebase origin/develop
# 테스트/생성물 검증
git push -u origin feature/CODEX-TICKET-slug
```

### 병합과 이력 규칙

- `main`과 `develop`에 직접 push하지 않는다.
- 공유 브랜치인 `main`, `develop`, `release/*`는 rebase하거나 force-push하지 않는다.
- 개인 작업 브랜치만 PR 생성 전 rebase할 수 있다. 이미 다른 작업자가 사용하는 브랜치는 force-push하지 않는다.
- 티켓 하나는 가능하면 의미 있는 커밋 하나로 만들고, PR 병합은 기본적으로 squash를 사용한다.
- feature 간 직접 병합하지 않는다. 공통 변경은 먼저 별도 PR로 `develop`에 반영한 뒤 양쪽이 다시 rebase한다.
- 준비되지 않은 브랜치를 “모두 합친다”는 이유로 병합하지 않는다. 현재 배포 범위에 포함되고 검증을 통과한 작업만 release에 포함한다.

### Cursor와 Codex의 인계 규칙

작업을 넘길 때 PR 또는 티켓에 다음을 남긴다.

- 기준 브랜치와 시작 커밋 SHA
- 변경 파일과 변경 이유
- 만족한 FR·AC
- 실행한 테스트와 결과
- 남은 문제와 열린 질문
- 생성 파일 여부와 재생성 명령

다른 AI가 이어서 작업할 때는 이전 AI의 브랜치에 바로 커밋하지 않는다. 해당 PR을 이어받는다고 사용자가 명시한 경우에만 같은 브랜치를 사용하고, 그 외에는 최신 `develop`에서 새 티켓 브랜치를 만든다.

### 충돌 처리

- 자동으로 한쪽 변경을 버리지 않는다.
- 스펙 ID 중복, 제품 원칙 충돌, 열린 질문에 대한 상반된 결정은 사용자에게 보고하고 판단을 기다린다.
- 코드 충돌은 각 변경의 관련 FR·AC를 확인한 뒤 담당자가 해결하고 다른 AI가 재검토한다.
- 충돌 해결 후 관련 테스트와 생성물 검증을 다시 실행한다.

### 최종 통합 게이트

GitHub의 `main`에 올리기 전에 기본 통합 담당자인 Codex가 다음을 확인한다.

- 이번 release에 포함할 모든 승인된 PR이 `develop`에 병합되었는가
- 미해결 충돌과 열린 차단 이슈가 없는가
- 스펙과 구현이 일치하고 FR·AC 추적이 가능한가
- 테스트, 린트, 빌드가 통과했는가
- 생성 파일이 원본과 일치하는가
- 민감정보, 절대 경로, 비밀키가 포함되지 않았는가
- `release/*`가 최신 `develop`을 포함하는가

하나라도 확인되지 않으면 `main` 병합과 태그 생성을 멈추고 사용자에게 보고한다.

## 여러 에이전트가 함께 작업할 때

- **한 번에 스펙 파일 하나만 맡는다.** 도메인 단위로 나누면 충돌이 거의 없다. 여러 도메인에 걸치는 변경은 사용자에게 조율을 요청한다.
- **`specs/000-product/spec.md`는 공유 파일이다.** 여기를 고칠 때는 다른 작업과 겹치지 않는지 먼저 확인한다.
- **요구사항 ID로 대화한다.** “인사 쪽 그거”가 아니라 `FR-010`처럼 쓴다.
- **번호를 재사용하지 않는다.** 새 항목은 그 파일의 마지막 번호 다음부터 붙인다.
- **커밋 메시지에는 무엇을 왜 바꿨는지 적는다.**

## 도구별 규칙 파일

- `AGENTS.md` (이 파일) — 공용 표준. Cursor, Codex, Copilot, Gemini CLI, Windsurf, Aider, Zed 등이 읽는다.
- `CLAUDE.md` — Claude Code는 AGENTS.md를 읽지 않는다. 이 파일에서 `@AGENTS.md`로 불러온다.
- `.cursor/rules/*.mdc` — Cursor 전용. glob 범위를 지정할 수 있어 스펙 파일에만 적용되는 규칙을 둔다.

내용이 갈라지지 않도록 공용 규칙은 이 파일에만 쓰고, 나머지는 참조하게 둔다.
